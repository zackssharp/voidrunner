# Anthropic API key for the /support AI assistant (server-side only).
# Get one at https://console.anthropic.com/. Copy this file to .env.local
# and put your real key there — never in this committed example file.
ANTHROPIC_API_KEY=
